import React from 'react';

export const ImageBackground = (props) => {
  return <div style={{ position: 'relative', display: 'flex', alignItems: 'center', justifyContent: 'center' }} className={props.className}></div>;
};
